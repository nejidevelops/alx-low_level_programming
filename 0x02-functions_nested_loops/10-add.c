#include <stdio.h>
#include "main.h"

/**
 * add - adds two numbers
 * @x: first number to add
 * @y: second number to add
 *
 * Return: return result of adding the numbers
 */
int add(int x, int y)
{
	return (x + y);
}
